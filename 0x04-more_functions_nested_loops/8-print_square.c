#include "main.h"

/**
 * The program to multiply two numbers
 */

void print_square (int m)
{
	a, b;
	if (m <= 0)
	{
		_putchar ('\n')
		{
		else
		{
		for ( a = 0; a < m; a++)
		{
		for ( b = 0; b < m; b++)
		} 
		_putchar (35);
		}
		_putchar ('\n');
		}
	}
}
